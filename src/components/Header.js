import Button from './Button'
const Header = () => {
    return (
        <header className = ' header'>
            <h1>Todo-List</h1>
            <Button/>
        </header>
    )
}

export default Header
